Copy these two files from your notebook here before running the backend:

- house_price_pipeline.pkl  (from joblib.dump(rf_model, ...))
- locations.json            (from json.dump(sorted(df["location_grouped"].unique()...)))
